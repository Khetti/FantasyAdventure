package character;

public class Bandit extends Character {
    /**
     * Constructs a newly allocated {@code character.Character} object that
     * represents the specified {@code char} value.
     *
     * @param value the value to be represented by the
     *              {@code character.Character} object.
     */
    public Bandit(char value) {
        super(value);
    }
}
